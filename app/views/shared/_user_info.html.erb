<a href="<%= user_path(current_user) %>">
  <%= gravatar_for current_user, size: 50 %>
</a>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to pluralize(current_user.microposts.count, "micropost"), current_user %>
</span>
